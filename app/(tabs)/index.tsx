import React from "react";

import { ChapterHub } from "@/components/ChapterHub";

// The Learn journey IS the home screen. Tap "Continue" to jump to the next lesson.
export default function HomeScreen() {
  return <ChapterHub />;
}
